{
  "name": "@leafbridge/ui-kit",
  "version": "0.0.1",
  "description": "React components shared by LeafBridge apps. Apple-iOS-feeling defaults.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "echo 'no lint configured'"
  }
}
