{
  "name": "@leafbridge/specialty-dsl",
  "version": "0.0.1",
  "description": "JSON Schema + Zod definitions for the LeafBridge extensions to the Specialty Template manifest: agents, clinical routing rules, write-back policy.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./schemas/manifest-extensions.json": "./schemas/manifest-extensions.json"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "echo 'no lint configured'"
  },
  "dependencies": {
    "zod": "^3.23.0"
  }
}
