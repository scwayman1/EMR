{
  "name": "@leafbridge/agent-sdk",
  "version": "0.0.1",
  "description": "Agent descriptor types, autonomy tiers, output schema, and registry helpers for LeafBridge agents.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run",
    "lint": "echo 'no lint configured'"
  },
  "dependencies": {
    "@leafbridge/specialty-dsl": "workspace:*",
    "zod": "^3.23.0"
  }
}
