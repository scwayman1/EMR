# API Route Auth Manifest — single source of truth for /api/** auth posture.
#
# Generated 2026-05-09 against origin/main HEAD e2ec08a.
#
# Every route under src/app/api/**/route.ts must have an entry. Adding a
# new route without updating this file fails CI (see scripts/check-route-auth-manifest.mjs).
#
# Schema:
#   routes:
#     <route-path>:               # Path relative to /api, e.g. "admin/super-admins"
#       methods: [GET, POST]      # Methods this route handles
#       auth: <kind>              # See "Auth kinds" below
#       owner: <team>             # Team responsible — drives PR review routing
#       notes: <string>           # Optional — anything a reviewer needs to know
#       review_due: <date>        # Optional — for routes flagged needs_review,
#                                 #   the date by which the classification must
#                                 #   be resolved.
#       rate_limit_bucket: <name> # Optional — bucket label passed to
#                                 #   withAdminMutation (EMR-728). Required de
#                                 #   facto for every mutation route under
#                                 #   /api/admin/** and /api/configs/**;
#                                 #   scripts/check-admin-mutation-coverage.mjs
#                                 #   enforces the helper wrapping at the source
#                                 #   level. Field here exists for legibility.
#
# Auth kinds:
#   required        — Authenticated user is a hard prerequisite.
#                     Helper must appear in the file: requireApiAuth,
#                     requireUser, requireRole, requireSuperAdmin,
#                     requireImplementationAdmin, requireControllerAccess,
#                     getCurrentUser, currentUser, or auth().
#
#   public          — Intentionally public. Document why in notes.
#                     Examples: marketing pages, OG image, shared catalogs.
#                     Use this kind ONLY when there is a deliberate product
#                     reason to expose data without auth.
#
#   webhook         — Inbound webhook from a third party. Must verify the
#                     signature/HMAC of the upstream provider. Routes that
#                     don't are security bugs (see PR #233).
#
#   cron            — Inbound cron tick. Must validate a shared secret in
#                     a header (X-Cron-Secret or equivalent) — being
#                     callable from the open internet is the failure mode
#                     this category is supposed to surface.
#
#   token_url       — Auth comes from an unguessable token in the URL or
#                     query (emergency access, share links). Token must
#                     be opaque, single-use or short-lived, and audited.
#
#   establishes_auth — Sign-in / sign-up / login / logout endpoints. Public
#                     by definition; must rate-limit and protect the
#                     credentials they receive.
#
#   needs_review    — Classification not yet determined. CI does NOT block
#                     on these (the manifest entry exists), but `review_due`
#                     must be filled and the date must not be past.
#
# Process:
#   1. Add a new API route → add an entry here in the same PR.
#   2. CI fails if a route lacks an entry, OR if an entry's `auth` field is
#      `needs_review` and `review_due` is past.
#   3. Changing a route's auth kind requires a security-review label on
#      the PR.

generated_at: 2026-05-17
generated_against: feature/wave-9-sprint@709f969
total_routes: 106

routes:

  # ── /api/admin/** ────────────────────────────────────────
  # All admin routes use requireApiAuth({ role: "super_admin" }) per PR #239
  # and run behind the origin-check middleware per PR #238.
  admin/super-admins:
    methods: [GET, POST]
    auth: required
    owner: platform-security
    notes: "super_admin role. CSRF defense via /api/admin/** origin middleware. POST behind withAdminMutation (EMR-728)."
    rate_limit_bucket: admin.super_admin.grant

  admin/super-admins/[userId]:
    methods: [DELETE]
    auth: required
    owner: platform-security
    notes: "super_admin role. Last-admin guard + Serializable txn (PR #225). DELETE behind withAdminMutation (EMR-728)."
    rate_limit_bucket: admin.super_admin.revoke

  admin/super-admins/[userId]/emergency-revoke:
    methods: [POST]
    auth: required
    owner: platform-security
    notes: "super_admin role. EMR-727 — strips role and adds target to SessionKillList for fleet-wide <1s session kill. Two-person rule (cannot revoke self unless co-admin issues)."
    rate_limit_bucket: admin.super_admin.emergency_revoke

  admin/impersonate/[practiceId]:
    methods: [POST]
    auth: required
    owner: platform-security
    notes: "super_admin role. EMR-742 — open 'view as practice' impersonation. Signed HMAC cookie, 30-min TTL, bound to actor user id. Emits super_admin.impersonation.enter audit row."
    rate_limit_bucket: admin.super_admin.impersonate

  admin/impersonate/exit:
    methods: [POST]
    auth: required
    owner: platform-security
    notes: "super_admin role. EMR-742 — clear impersonation cookie. Always allowed (even during impersonation read-only) so user can exit."

  admin/practices:
    methods: [GET]
    auth: required
    owner: platform-security
    notes: "super_admin role. Read-only listing."

  admin/search:
    methods: [GET]
    auth: required
    owner: platform-security
    notes: "super_admin role. EMR-738 cross-tenant search. Query `q` is redacted before audit persistence: emails (contain '@') and phone-shaped strings (>=7 digits) are truncated to first 3 chars + '***'. Indexed columns only; no full-text. Emits controller.super_admin.cross_tenant_search audit row per request."

  admin/audit:
    methods: [GET]
    auth: required
    owner: platform-security
    notes: "super_admin role. EMR-747 ControllerAuditLog viewer. Cursor-paginated (no offset). Reads only — does NOT emit an audit row per request (would create a feedback loop with the table it lists). Filters: actor, action (case-insensitive contains), target, from/to. Metadata preview masks email/phone shapes; full JSON on expand renders verbatim (super-admin surface only)."

  admin/audit/export:
    methods: [GET]
    auth: required
    owner: platform-security
    notes: "super_admin role. EMR-747 streaming CSV export of ControllerAuditLog. Uses streamCsvResponse from src/lib/admin/csv-export.ts (EMR-749) — one super_admin.csv_export audit row emitted BEFORE the stream begins; rows pulled lazily via async iterator so a 100k-row export never materialises in memory. Includes a practice_id column to satisfy the utility's requirePracticeIdColumn enforcement."

  admin/practices/[id]/switch-specialty:
    methods: [POST]
    auth: required
    owner: platform-security
    notes: "super_admin role. Requires status=published guard (PR #225). POST behind withAdminMutation (EMR-728)."
    rate_limit_bucket: admin.config.switch_specialty

  # ── Onboarding controller (EMR-428) ───────────────────────
  audit:
    methods: [GET]
    auth: required
    owner: platform-security
    notes: "implementation_admin role. ControllerAuditLog read access."

  configs:
    methods: [POST]
    auth: required
    owner: onboarding-controller
    notes: "implementation_admin via withAdminMutation (EMR-728)."
    rate_limit_bucket: admin.config.draft_create

  configs/[id]:
    methods: [GET, PATCH]
    auth: required
    owner: onboarding-controller
    notes: "PATCH wrapped by withAdminMutation (EMR-728)."
    rate_limit_bucket: admin.config.update

  configs/[id]/publish:
    methods: [POST]
    auth: required
    owner: onboarding-controller
    notes: "implementation_admin via withAdminMutation (EMR-728)."
    rate_limit_bucket: admin.config.publish

  configs/[id]/archive:
    methods: [POST]
    auth: required
    owner: onboarding-controller
    notes: "implementation_admin via withAdminMutation (EMR-728)."
    rate_limit_bucket: admin.config.archive

  configs/[id]/upgrade-template:
    methods: [POST]
    auth: required
    owner: onboarding-controller
    notes: "implementation_admin via withAdminMutation (EMR-728)."
    rate_limit_bucket: admin.config.upgrade_template

  configs/[id]/apply-specialty:
    methods: [POST]
    auth: required
    owner: onboarding-controller
    notes: "implementation_admin via withAdminMutation (EMR-728). Earlier 2026-05-12 entry noted middleware coarse-gating; the route is now self-gated too."
    rate_limit_bucket: admin.config.apply_specialty

  configs/by-practice/[practiceId]:
    methods: [GET]
    auth: required
    owner: onboarding-controller
    notes: "Uses getCurrentUser. Verify it also enforces practice-scope via canViewPracticeConfig."

  practices:
    methods: [GET, POST]
    auth: required
    owner: onboarding-controller

  migration-profiles:
    methods: [POST]
    auth: required
    owner: onboarding-controller

  migration-profiles/[id]:
    methods: [GET]
    auth: required
    owner: onboarding-controller

  orgs:
    methods: [GET, POST]
    auth: required
    owner: onboarding-controller

  specialty-templates:
    methods: [GET]
    auth: public
    owner: onboarding-controller
    notes: "Closed 2026-05-12 via find-and-fix pass 4. Returns specialty metadata only (slug, name, description, version). No PHI; safe public — needed by the public onboarding wizard."

  # ── Agents (LLM-backed, $$ implications) ─────────────────
  agents/pharmacology:
    methods: [POST]
    auth: required
    owner: agents
    notes: "Closed 2026-05-09: requireApiAuth + agentInvocationLimiter (30/min/actor). Bucket: agent.pharmacology. Streams SSE; gate runs BEFORE the body is consumed."

  cindy:
    methods: [POST]
    auth: required
    owner: agents
    notes: "Closed 2026-05-09: requireApiAuth + agentInvocationLimiter (30/min/actor). Bucket: agent.cindy."

  cfo/generate:
    methods: [GET, POST]
    auth: cron
    owner: agents
    notes: "Cron-style. POST validates Bearer CRON_SECRET, fail-closed in prod when env unset (PR S — same hardening pattern as cron/reminders). Org fan-out capped at 250."

  feedback/whisper:
    methods: [GET, POST]
    auth: public
    owner: agents
    notes: "EMR-594. Universal feedback FAB mounted in the root layout — fires from every surface including signed-out marketing/leafmart. Logged-in user info is attached when present via getCurrentUser(); anonymous submissions are accepted by design so storefront visitors can also reach the founders. The earlier 'unmetered audio transcription' concern no longer applies — text-only since EMR-365. Follow-up: rate-limit + per-IP cap before scale."

  # ── Imaging (PHI) ────────────────────────────────────────
  imaging/studies:
    methods: [GET]
    auth: required
    owner: imaging
    notes: "Closed 2026-05-12 via PR fix/imaging-routes-add-auth. Find-and-fix pass 4 confirmed this route returned a list of imaging studies including patientId, modality, body part, and indication to anonymous callers. Now gated with requireApiAuth()."

  imaging/studies/[id]:
    methods: [GET]
    auth: required
    owner: imaging
    notes: "Closed 2026-05-12 via PR fix/imaging-routes-add-auth. Gate added at top of handler."

  imaging/studies/[id]/annotations:
    methods: [GET, POST, DELETE]
    auth: required
    owner: imaging
    notes: "Closed 2026-05-12 via PR fix/imaging-routes-add-auth. All three handlers now gate before reading state."

  imaging/upload:
    methods: [GET, POST]
    auth: required
    owner: imaging
    notes: "Uses getCurrentUser."

  # ── Patient-facing data ───────────────────────────────────
  appointments/ical:
    methods: [GET]
    auth: required
    owner: scheduling
    notes: "Uses getCurrentUser. Returns user's calendar."

  export/outcomes:
    methods: [GET]
    auth: required
    owner: research

  transcribe:
    methods: [POST]
    auth: required
    owner: scribe

  # ── Leafmart commerce ─────────────────────────────────────
  leafmart/me:
    methods: [GET]
    auth: required
    owner: leafmart

  leafmart/cart:
    methods: [GET, POST, DELETE]
    auth: required
    owner: leafmart

  leafmart/cart/share:
    methods: [POST]
    auth: public
    owner: leafmart
    review_due: 2026-05-23
    notes: "Generates shareable cart link. Public access likely intentional; confirm rate limit + token opacity."

  leafmart/addresses:
    methods: [GET, POST]
    auth: required
    owner: leafmart

  leafmart/addresses/[id]:
    methods: [DELETE, PATCH]
    auth: required
    owner: leafmart

  leafmart/checkout:
    methods: [POST]
    auth: required
    owner: leafmart

  leafmart/account/affirmations:
    methods: [POST]
    auth: public
    owner: leafmart
    review_due: 2026-05-30
    notes: "🚨 Patient-bound POST, no auth detected. Likely needs auth. Review_due extended 2026-05-17: leafmart commerce review is out of scope for wave-9 (EMR-679/680); will be addressed in the leafmart auth pass alongside questions/reviews."

  leafmart/products/[slug]/questions:
    methods: [POST]
    auth: public
    owner: leafmart
    review_due: 2026-05-30
    notes: "Posting a question — anonymous OK or requires user? Confirm. Review_due extended 2026-05-17: leafmart commerce review is out of scope for wave-9 (EMR-679/680)."

  leafmart/products/[slug]/reviews:
    methods: [POST]
    auth: public
    owner: leafmart
    review_due: 2026-05-30
    notes: "Posting a review — likely require auth + verified-purchase. Confirm. Review_due extended 2026-05-17: leafmart commerce review is out of scope for wave-9 (EMR-679/680)."

  leafmart/search:
    methods: [GET]
    auth: public
    owner: leafmart
    notes: "Public product search."

  leafmart/vendor-apply:
    methods: [POST]
    auth: public
    owner: leafmart
    notes: "Public vendor application form. Rate-limit recommended."

  leafmart/age-gate/consent:
    methods: [POST]
    auth: public
    owner: leafmart
    notes: "EMR-398. Storefront is anonymous; this audit-logs the 21+/T&C consent (IP + timestamp + UA) so we have compliance evidence without storing PII at rest."

  leafmart/signout:
    methods: [POST]
    auth: establishes_auth
    owner: leafmart
    notes: "Clears Leafmart session cookies."

  # ── Marketplace ───────────────────────────────────────────
  marketplace/age-gate/confirm:
    methods: [POST]
    auth: required
    owner: leafmart

  marketplace/coa/[documentId]:
    methods: [GET]
    auth: public
    owner: leafmart
    notes: "Certificate of Analysis viewer — intentionally public for transparency."

  marketplace/tax/calculate:
    methods: [POST]
    auth: public
    owner: leafmart
    review_due: 2026-05-23
    notes: "Pre-checkout tax calc. Public OK if rate-limited; otherwise an enumeration vector."

  marketplace/vendors/[vendorId]/documents/coa:
    methods: [POST]
    auth: required
    owner: leafmart

  marketplace/vendors/[vendorId]/documents/w9:
    methods: [GET, POST]
    auth: required
    owner: leafmart

  # ── Webhooks (signature-verified) ─────────────────────────
  webhooks/clerk:
    methods: [POST]
    auth: webhook
    owner: platform-security
    notes: "svix signature, fail-closed on missing secret/headers."

  webhooks/payabli:
    methods: [POST]
    auth: webhook
    owner: billing
    notes: "Per PR #233: gateway must be 'payabli' in prod, signature verified unconditionally."

  webhooks/payabli/marketplace:
    methods: [POST]
    auth: webhook
    owner: leafmart
    notes: "HMAC-SHA256 timing-safe verification, fail-closed on missing secret."

  # ── Cron (inbound triggers) ───────────────────────────────
  cron/anomaly-sweep:
    methods: [POST]
    auth: cron
    owner: platform-security
    rate_limit_bucket: cron.anomaly-sweep
    notes: "EMR-734. Bearer CRON_SECRET check, prod-only (non-prod falls through — same pattern as cron/audit-export). Runs registered anomaly detectors and upserts results into the Anomaly table; auto-resolves rows the detector stops emitting and TTL-expires stale rows. Registry is intentionally empty until EMR-737/740/741 land — sweep returns 200 with detectorsRun=0 in the interim."

  cron/audit-export:
    methods: [POST]
    auth: cron
    owner: platform-security
    notes: "EMR-750. Bearer CRON_SECRET check, prod-only (non-prod falls through — same pattern as cron/credential-check). Exports previous UTC day of ControllerAuditLog as gzipped JSONL to audit/controller/YYYY/MM/DD.jsonl.gz. Idempotent via ControllerAuditExport.coveredDate unique key."

  cron/coa-tracker:
    methods: [POST]
    auth: cron
    owner: leafmart
    notes: "Closed 2026-05-12 via find-and-fix pass 4. Probe returned 401 unauthorized — handler validates a cron secret header. Audit assumption was wrong; the gate exists."

  cron/reminders:
    methods: [GET]
    auth: cron
    owner: scheduling
    notes: "Closed via PR #243 — prod hard-refuses without CRON_SECRET, non-prod logs and falls through (200 in dev). Find-and-fix pass 4 confirmed dev fall-through; prod path is gated by NODE_ENV check."

  cron/practice-health:
    methods: [POST]
    auth: cron
    owner: super-admin
    notes: "EMR-732. Bearer CRON_SECRET, prod-only fail-closed, dev fall-through (matches cron/credential-check). Per-org cooldown via PRACTICE_HEALTH_MIN_AGE_SECONDS (default 300s); batch capped at 200 organizations per invocation."

  # ── Token-URL access ──────────────────────────────────────
  emergency/[token]:
    methods: [GET]
    auth: token_url
    owner: clinical-safety
    notes: "Break-glass emergency-access token. Verify token table + expiry + audit on read."

  share/og:
    methods: [GET]
    auth: public
    owner: marketing
    notes: "Open Graph image generation, public by definition."

  share:
    methods: [POST]
    auth: required
    owner: marketing
    review_due: 2026-05-23
    notes: "Share-link creation. Verify it requires an authed user to mint a share token."

  # ── Vendor portal auth ────────────────────────────────────
  vendor-portal/auth/login:
    methods: [POST]
    auth: establishes_auth
    owner: leafmart
    notes: "Vendor login. Must rate-limit + audit failed attempts."

  vendor-portal/auth/logout:
    methods: [POST]
    auth: establishes_auth
    owner: leafmart

  vendor-portal/auth/totp/enroll:
    methods: [POST]
    auth: required
    owner: leafmart
    notes: "Closed 2026-05-12 via find-and-fix pass 4. Probe returned 401 no_session — handler requires an established vendor session."

  # ── Public infra ──────────────────────────────────────────
  health:
    methods: [GET]
    auth: public
    owner: sre
    notes: "Liveness check. No data, no DB query."

  platform/licensing/menu.html:
    methods: [GET]
    auth: public
    owner: marketing
    notes: "Public licensing menu HTML."

  platform/licensing/menu.json:
    methods: [GET]
    auth: public
    owner: marketing
    notes: "Public licensing menu JSON."

  reports/competitive-analysis:
    methods: [GET]
    auth: required
    owner: research
    notes: "Closed 2026-05-12 via PR fix/competitive-analysis-route-auth. Find-and-fix pass 4 confirmed full report was returned unauthed. Now gated requireApiAuth({role:'super_admin'})."

  dispensary/ingest:
    methods: [POST]
    auth: required
    owner: integrations
    notes: "Closed 2026-05-12 via find-and-fix pass 4. Probe returned 401 unauthorized — gate exists."

  integrations/linear/issues/[identifier]:
    methods: [GET]
    auth: required
    owner: integrations
    notes: "Uses requireUser. Surfaces Linear issue context."

  contact:
    methods: [POST]
    auth: public
    owner: marketing
    notes: "Public contact form submission route."

  audit/report:
    methods: [GET]
    auth: cron
    owner: platform-security
    review_due: 2026-05-30
    notes: "Scaffolded route for audit reporting."

  foundation/grants:
    methods: [GET, POST]
    auth: public
    owner: marketing
    review_due: 2026-05-30
    notes: "Scaffolded route for foundation grants."

  integrations/test-garmin:
    methods: [POST]
    auth: webhook
    owner: integrations
    review_due: 2026-05-30
    notes: "Scaffolded route for testing Garmin integration."

  portal/encounters/[id]/calendar.ics:
    methods: [GET]
    auth: required
    owner: scheduling
    notes: "Patient calendar ICS download."
  cron/sync-wearables:
    methods: [GET]
    auth: cron
    owner: platform-security
    notes: "Cron daemon for syncing wearables."

  # ── Wave 9 — clinical & integrations scaffolds ────────────
  # Added 2026-05-17 with EMR-679/EMR-680 (PR #337). Routes are scaffolded
  # behind feature flags; auth posture documented here, with needs_review
  # flagged for any handler whose gate is incomplete (Bearer-prefix only,
  # missing secret comparison, or no auth at all).

  agents/chat-cb:
    methods: [POST]
    auth: webhook
    owner: agents
    review_due: 2026-05-30
    notes: "EMR-679 scaffold. Public LLM passthrough (OpenRouter) — no auth, no rate limit. Decide intended audience (Education tab public vs providers-only) and add agentInvocationLimiter before broad exposure. $$ risk."

  agents/claims-scrubber:
    methods: [POST]
    auth: cron
    owner: agents
    review_due: 2026-05-30
    notes: "EMR-045 scaffold. Background agent mutates Claim rows; currently no auth gate. Should run behind CRON_SECRET (match inventory-restock pattern) before merge to prod."

  agents/clinical-decision-support:
    methods: [POST]
    auth: webhook
    owner: agents
    review_due: 2026-05-30
    notes: "EMR-055 scaffold. Reads PHI (allergies, contraindications). Prod check only validates that Authorization starts with 'Bearer ' — no token comparison. Tighten to either requireApiAuth or a compared secret."

  agents/inventory-restock:
    methods: [POST]
    auth: cron
    owner: agents
    notes: "EMR-052 scaffold. Bearer CRON_SECRET check, prod-only (non-prod falls through — same pattern as cron/reminders)."

  agents/lab-results-parser:
    methods: [POST]
    auth: webhook
    owner: agents
    notes: "EMR-056 scaffold. Inbound from lab provider. Bearer LAB_PARSER_SECRET check, prod-only. Confirm secret-rotation policy when wiring real LabCorp/Quest feeds."

  agents/waitlist-manager:
    methods: [POST]
    auth: cron
    owner: agents
    notes: "EMR-043 scaffold. Bearer CRON_SECRET check, prod-only. Notifies waitlisted patients of recent cancellations."

  audit/dea-logs:
    methods: [POST]
    auth: required
    owner: platform-security
    notes: "EMR-041 scaffold. Uses requireUser. DEA controlled-substance audit log writer."

  cron/leafly-sync:
    methods: [POST]
    auth: cron
    owner: integrations
    review_due: 2026-05-30
    notes: "Wave-9 scaffold. POST handler has no secret check; sibling cron/sync-leafly does. Either fold into sync-leafly or add CRON_SECRET gate."

  cron/sync-leafly:
    methods: [GET]
    auth: cron
    owner: integrations
    notes: "Wave-9 scaffold. Bearer CRON_SECRET compared on every request — fail-closed when env unset matches recommended pattern."

  integrations/fhir/ingest:
    methods: [POST]
    auth: webhook
    owner: integrations
    notes: "EMR-013 scaffold. Bearer FHIR_INGEST_SECRET check, prod-only. Inbound HL7/FHIR Bundle from Epic/Cerner."

  integrations/prohub/sync:
    methods: [POST]
    auth: webhook
    owner: integrations
    notes: "EMR-053 scaffold. Bearer PROHUB_SYNC_SECRET check, prod-only. Inbound product/sales sync from ProHub dispensaries."

  integrations/referrals/transmit:
    methods: [POST]
    auth: required
    owner: integrations
    notes: "EMR-061 scaffold. Uses requireUser. Provider-initiated outbound referral (CCDA + Direct Secure Messaging stub)."

  integrations/surescripts/transmit:
    methods: [POST]
    auth: webhook
    owner: integrations
    review_due: 2026-05-30
    notes: "EMR-015 scaffold. Prod check only validates that Authorization starts with 'Bearer ' — no token comparison. Strengthen before any real e-prescribe traffic."

  integrations/twilio/video-token:
    methods: [GET]
    auth: required
    owner: integrations
    notes: "EMR-024 scaffold. Uses requireUser. Issues a Twilio access token tied to user.id."

  mobile/biometrics/sync:
    methods: [POST]
    auth: webhook
    owner: mobile
    review_due: 2026-05-30
    notes: "EMR-051 scaffold. Only checks Authorization starts with 'Bearer ' — no JWT validation. Must verify token before accepting biometric writes from the mobile app."

  patient/intake/submit:
    methods: [POST]
    auth: required
    owner: scheduling
    notes: "EMR-036 scaffold. Uses requireUser; also asserts the encounter belongs to the current patient before writing intake."

  reports/mips-export:
    methods: [GET]
    auth: cron
    owner: research
    review_due: 2026-05-30
    notes: "EMR-042 scaffold. Returns aggregated PHI-derived MIPS metrics as XML; currently no auth gate. Add requireApiAuth (likely super_admin or org_admin scope) before exposing."

  agents/billing-coder:
    methods: [POST]
    auth: cron
    owner: agents
    notes: "EMR-032 scaffold. Bearer CRON_SECRET check, prod-only. Background coder that drafts Claim rows from signed encounter notes."

  agents/prior-auth:
    methods: [POST]
    auth: cron
    owner: agents
    notes: "EMR-046 scaffold. Bearer CRON_SECRET check, prod-only. Generates draft PriorAuthorization records when a medication triggers payer rules."

  cron/outreach:
    methods: [POST]
    auth: cron
    owner: agents
    notes: "EMR-033 scaffold. Bearer CRON_SECRET check, prod-only. Renewal/wellness outreach to patients with expiring qualifications."

  mobile/kiosk/check-in:
    methods: [POST]
    auth: webhook
    owner: mobile
    notes: "EMR-029 scaffold. Bearer KIOSK_SECRET check, prod-only. Inbound check-in events from the front-desk iPad kiosk."

  agents/patient-education:
    methods: [POST]
    auth: cron
    owner: agents
    notes: "EMR-065 scaffold. Bearer CRON_SECRET check, prod-only. Generates post-visit education summaries from signed encounter notes."

  agents/rcm-denial-analyzer:
    methods: [POST]
    auth: cron
    owner: agents
    notes: "EMR-073 scaffold. Bearer CRON_SECRET check, prod-only. Scans denied claims and queues draft appeals."

  agents/trial-matcher:
    methods: [POST]
    auth: cron
    owner: agents
    notes: "EMR-072 scaffold. Bearer CRON_SECRET check, prod-only. Cross-matches patient cohorts against ClinicalTrials.gov."

  agents/voice-transcriber:
    methods: [POST]
    auth: required
    owner: scribe
    notes: "EMR-070 scaffold. Uses requireUser. Accepts an audio blob and returns a SOAP-structured transcript."

  cron/credential-check:
    methods: [POST]
    auth: cron
    owner: platform-security
    notes: "EMR-068 scaffold. Bearer CRON_SECRET check, prod-only. Weekly NPPES/DEA license verification sweep."

  cron/erx-ddi-sync:
    methods: [POST]
    auth: cron
    owner: integrations
    notes: "EMR-067 scaffold. Bearer CRON_SECRET check, prod-only. Nightly First Databank / Medispan DDI table refresh."

  integrations/dsm/ingest:
    methods: [POST]
    auth: webhook
    owner: integrations
    notes: "EMR-069 scaffold. Bearer DSM_INGEST_SECRET check, prod-only. Inbound Direct Secure Messaging CCDA payloads."

  integrations/fleet-delivery:
    methods: [POST]
    auth: webhook
    owner: integrations
    notes: "EMR-074 scaffold. Bearer FLEET_WEBHOOK_SECRET check, prod-only. Inbound driver GPS / delivery status events from Onfleet/Bringg."

  integrations/pharmacy/webhook:
    methods: [POST]
    auth: webhook
    owner: integrations
    notes: "EMR-071 scaffold. Bearer PHARMACY_WEBHOOK_SECRET check, prod-only. Inbound fill-status events from specialty pharmacies."

  integrations/vitals-sync:
    methods: [POST]
    auth: webhook
    owner: integrations
    notes: "EMR-066 scaffold. Bearer IOT_WEBHOOK_SECRET check, prod-only. Inbound vitals from connected medical devices (Validic-style aggregators)."

  agents/attrition-analyzer:
    methods: [POST]
    auth: cron
    owner: agents
    notes: "EMR-079 scaffold. Bearer CRON_SECRET check, prod-only. Scores patient churn risk from cancellation history."

  agents/inbox-triage:
    methods: [POST]
    auth: webhook
    owner: agents
    notes: "EMR-080 scaffold. Bearer WEBHOOK_SECRET check, prod-only. Categorizes inbound patient messages and tags urgency."

  agents/pgx-risk-scorer:
    methods: [POST]
    auth: cron
    owner: agents
    notes: "EMR-077 scaffold. Bearer CRON_SECRET check, prod-only. Cross-references genetic markers against active meds."

  agents/protocol-generator:
    methods: [POST]
    auth: cron
    owner: agents
    notes: "EMR-075 scaffold. Bearer CRON_SECRET check, prod-only. Drafts 6-month step-therapy protocols from a primary diagnosis."

  cron/cs-reconciliation:
    methods: [POST]
    auth: cron
    owner: platform-security
    notes: "EMR-076 scaffold. Bearer CRON_SECRET check, prod-only. Nightly controlled-substance vault-vs-dispense reconciliation."

  integrations/state-registry/sync:
    methods: [POST]
    auth: webhook
    owner: integrations
    notes: "EMR-078 scaffold. Bearer STATE_REGISTRY_SECRET check, prod-only. Outbound METRC/BioTrack/PDMP compliance reporting."

  cron/mutation-budget-sweep:
    methods: [POST]
    auth: cron
    rate_limit_bucket: cron.mutation-budget
    owner: platform-security
    notes: "EMR-730. Bearer CRON_SECRET check, prod-only. Sweeps ControllerAuditLog over a 5-minute window, fires a logger.error alarm (event: super_admin.mutation_budget_exceeded) when any actor exceeds 30 mutations/min OR 100 mutations/5min. Per-actor dedupe ledger: MutationBudgetAlarm, 10-minute window. Threshold env knobs: SUPER_ADMIN_ALARM_PER_MIN, SUPER_ADMIN_ALARM_PER_5MIN."

  terminology/[system]/[code]:
    methods: [GET]
    auth: public
    owner: integrations
    notes: "EMR-766 terminology lookup endpoint. Pure public reference data (LOINC, SNOMED, RxNorm, ICD10 lookup), no PHI, safe public."
